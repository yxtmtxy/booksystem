<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yx.dao.AdminMapper">
    <resultMap id="BaseResultMap" type="com.yx.pojo.Admin">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Mar 30 16:30:04 CST 2021.
        -->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="adminType" jdbcType="INTEGER" property="adminType" />
    </resultMap>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select *
        from admin
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="queryAdminByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        select *
        from admin
        where username = #{username,jdbcType=VARCHAR}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from admin
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.yx.pojo.Admin">
        insert into admin (id,username, password, adminType)
        values (#{id,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR},
                #{password,jdbcType=VARCHAR},#{adminType,jdbcType=INTEGER})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.yx.pojo.Admin">
        update admin
        set username = #{username,jdbcType=VARCHAR},
        password = #{password,jdbcType=VARCHAR},
        adminType = #{adminType,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>

    <select id="queryAdminInfoAll" parameterType="com.yx.pojo.Admin" resultType="com.yx.pojo.Admin">
        SELECT * from admin
        <where>
            <if test="username!=null">
                and username like '%${username}%'
            </if>
            <if test="adminType!=null">
                and adminType like '%${adminType}%'
            </if>
        </where>
    </select>

    <select id="queryUserByNameAndPassword" resultType="com.yx.pojo.Admin">
        select * from admin where username=#{username} and password=#{password}
    </select>
</mapper>